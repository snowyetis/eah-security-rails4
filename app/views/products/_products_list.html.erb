<%= render partial: 'product_top', locals: { product_top: @product_top } %>

<%= render partial: 'product_middle', locals: { product_middle: @product_middle } %>

<%= render partial: 'product_bottom', locals: { product_bottom: @product_bottom } %>
